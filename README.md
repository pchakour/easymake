# Emake

## When the cache is rebuild for a target ?

  - In files change
  - Out files change

## TODO

- Rebuild cache when the sha512 of an action change
- Lock an action execution to avoid parallel execution of the same action
- private target (not callable in command line) ?
- Enhance the generation of graph to work with the target plugin